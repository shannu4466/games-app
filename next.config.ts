import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['i.imgur.com', 'miro.medium.com', 'raw.githubusercontent.com','github.githubassets.com'],
  },
};

export default nextConfig;
